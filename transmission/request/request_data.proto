syntax = "proto3";

message requestQuery{
  string id = 1;
  string op = 2;
}

message results {
  bytes msg = 1;
}

service RequestTransmission {
  rpc RequestParsing (requestQuery) returns (results);
  rpc RequestDecrypt (results) returns(results);
}